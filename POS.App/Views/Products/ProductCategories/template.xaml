<Window x:Class="POS.App.Views.Products.ProductCategories.template"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="POS System" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Grid>
        
    </Grid>
</Window>








<!--<UserControl x:Class="POS.App.Views.Products.ProductCategories.template"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" xmlns:mah1="http://schemas.microsoft.com/netfx/2009/xaml/presentation" xmlns:mah2="http://schemas.microsoft.com/expression/blend/2008"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            --><!-- Title --><!--
            <RowDefinition Height="Auto"/>
            --><!-- Toolbar --><!--
            <RowDefinition Height="*"/>
            --><!-- DataGrid --><!--
        </Grid.RowDefinitions>

        --><!-- PAGE TITLE --><!--
        <TextBlock Text="Screen Title Here"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="0 0 0 15" />

        --><!-- Toolbar --><!--
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 15">

            --><!-- Search Bar --><!--
            <mah:TextBox Width="250" 
                         mah:TextBoxHelper.Icon="Magnify"
                         Watermark="Search..."
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            --><!-- Buttons --><!--
            <mah:Button Content="Add"
                        Width="90"
                        Command="{Binding AddCommand}" />

            <mah:Button Content="Update"
                        Width="90"
                        Command="{Binding UpdateCommand}" />

            <mah:Button Content="Delete"
                        Width="90"
                        Command="{Binding DeleteCommand}" />
        </StackPanel>

        --><!-- Data Grid --><!--
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0 5 0 0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100" />
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>-->
